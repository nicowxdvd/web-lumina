# Daily — 2026-09-18

## Qué hice
- Reubiqué en el backlog el checkbox "Acepto los términos y condiciones" de `LoginView`: no se movió código todavía, pero quedó anotado como parte del punto 6 (Registro) en `docs/pendientes.md`, en vez de quedar suelto solo en la conversación.
- Confirmé que en este proyecto todavía no existe ninguna vista de registro/alta de usuario (`LoginView` y `TerminosView` son las únicas por ahora).
- Definí que `docs/` en Lumina queda versionado en git tal cual está (excepción puntual a la regla global de no trackearlo), para no tener que volver a decidirlo cada vez.

## Qué sigue
- Arrancar la pantalla de Registro (punto 6 del backlog: `app/registro/page.tsx` + vista + use case + adaptador), que incluye mover ahí el checkbox de términos.

## Bloqueos
- Ninguno.
